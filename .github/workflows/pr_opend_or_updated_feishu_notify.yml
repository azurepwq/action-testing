name: Notify Feishu on New Pull Request or Update

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize]

jobs:
  notify-feishu:
    runs-on: ubuntu-latest

    steps:
      - name: Set ACTION environment variable
        run: echo "ACTION=$([[ '${{ github.event.action }}' == 'opened' ]] && echo 'New Pull Request' || echo 'Pull Request Updated')" >> $GITHUB_ENV

      - name: Send notification to Feishu
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          ACTION: ${{ env.ACTION }}
        if: github.event.action == 'opened' || github.event.action == 'synchronize'
        run: |
          curl -X POST -H "Content-Type: application/json" \
          -d '{
                "msg_type": "interactive",
                "card": {
                    "config": {
                        "wide_screen_mode": true
                    },
                    "header": {
                        "title": {
                            "tag": "plain_text",
                            "content": "${{ env.ACTION }}"
                        },
                        "template": "blue"
                    },
                    "elements": [
                        {
                            "tag": "div",
                            "text": {
                                "tag": "lark_md",
                                "content": "**Repository:** [${{ github.repository }}](https://github.com/${{ github.repository }})\n**Pull Request:** [#${{ github.event.pull_request.number }} - ${{ github.event.pull_request.title }}](${{ github.event.pull_request.html_url }})\n**Author:** ${{ github.event.pull_request.user.login }}\n\n${{ github.event.pull_request.body }}"
                            }
                        },
                        {
                            "tag": "action",
                            "actions": [
                                {
                                    "tag": "button",
                                    "text": {
                                        "tag": "plain_text",
                                        "content": "View Pull Request"
                                    },
                                    "url": "${{ github.event.pull_request.html_url }}",
                                    "type": "primary"
                                }
                            ]
                        }
                    ]
                }
              }' \
          $FEISHU_WEBHOOK_URL